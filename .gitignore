# Freebuff
.freebuff/

# Node (por si se reintenta un build step)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Artifacts viejos
web/
nul
server.log

# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/

# Python (validation tooling builds these; never commit)
__pycache__/
*.pyc
*.pyo

# Validation-tooling iteration scratch (one-off scripts + JSON dumps;
# see scripts/check-spec.py --help for the canonical toolchain)
_gate.json
_insert_*.py
_apply_*.py
_frebuff-validation/
.frebuff-validation/
.atl/
.lighthouserc.json
.idea/
*.swp
*.swo

# Documentos fuente (tesis, etc.) — no forman parte del sitio
*.docx
*.pdf

# One-off / archive helpers (move here instead of root)
scripts/_archive/

# Python (gate + scripts)
__pycache__/
*.pyc
*.pyo
*$py.class
